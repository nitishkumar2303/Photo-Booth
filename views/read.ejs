<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <link rel="stylesheet" href="../stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 min-h-screen">
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white flex flex-col items-center">
        <div class="nav mb-10 w-full">
            <a class="text-blue-500" href="/">Back to home</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>
        <div class="users flex gap-3 flex-wrap justify-center w-full">
            <% if (users.length > 0) { %>
                <% users.forEach(user => { %>
                    <article class="w-64 h-96 pt-5 pl-5 pr-5 pb-5 bg-zinc-800 rounded-lg">
                        <div class="w-full h-56 rounded-lg bg-red-500 overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= user.image %>" alt="Image of <%= user.name %>">
                        </div>
                        <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3 mt-5">
                            <a class="text-zinc-600" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%= user._id %>">Delete</a>
                        </div>
                    </article>
                <% }) %>
            <% } else { %>
                <p class="text-white">No users found.</p>
            <% } %>
        </div>
    </div>
</body>

</html>
